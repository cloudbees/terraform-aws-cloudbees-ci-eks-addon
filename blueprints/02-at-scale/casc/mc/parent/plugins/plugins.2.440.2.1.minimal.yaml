# This file is automatically generated - please do not edit manually.

# Annotations (given as a comment above the plugin in question):
#  tag:custom:version=...    - set a custom version (e.g. 1.0)
#  tag:custom:url=...        - sets a custom url (e.g. https://artifacts.acme.test/my-plugin/1.0/my-plugin.jpi)
#  tag:custom:requires=...   - spaced separated list of required dependencies (e.g. badge envinject)

# Plugin Categories:
#  cap - is this a CAP plugin?
#  3rd - is this a 3rd party plugin?
#  old - is this a deprecated plugin?
#  cve - are there open security issues?
#  bst - installed by default
#  dep - installed as dependency
#  src - used as a source plugin for this list

plugins:
  - id: audit-trail # 3rd src
  - id: bootstrap4-api # 3rd src
  - id: cloudbees-aborted-builds # cap src
  - id: cloudbees-casc-client # cap src
  - id: cloudbees-casc-items-controller # cap src
  - id: cloudbees-disk-usage-simple # 3rd src
  - id: cloudbees-groovy-view # cap src
  - id: cloudbees-pipeline-explorer # cap src
  - id: cloudbees-prometheus # 3rd src
  - id: cloudbees-s3-cache # cap src
  - id: copyartifact # cap src
  - id: font-awesome-api # cap dep src
  - id: github-branch-source # cap src
  - id: infradna-backup # cap src
  - id: javadoc # cap src
  - id: jsch # cap src
  - id: junit # cap dep src
  - id: ldap # cap src
  - id: managed-master-hibernation # cap src
  - id: mock-load-builder # 3rd src
  - id: momentjs # 3rd src
  - id: operations-center-notification # cap src
  - id: pipeline-event-step # cap src
  - id: pipeline-model-definition # cap src
  - id: pipeline-utility-steps # 3rd src
  - id: popper-api # 3rd dep src
  - id: popper2-api # cap src
  - id: sshd # cap src
  - id: timestamper # cap src
  - id: workflow-cps-checkpoint # cap src